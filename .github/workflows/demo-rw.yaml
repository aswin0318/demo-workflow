name: demo-rw
on:
  push:
    branches:
      - main

jobs:
  job1:
    uses: ./.github/workflows/ci_reusable.yml
    with:
      branch: ${{ github.ref_name }}
      actor: ${{ github.actor }}
      repo: ${{ github.repository }}

    
            
